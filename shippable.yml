---
branches:
  only:
    - master

base_image: shippableimages/ubuntu1404_base

cache: true

before_install:
  - curl -sSL https://get.docker.com/ | sh
  - env | grep DOCKER

install: true

script:
  - make test
