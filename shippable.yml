---
branches:
  only:
    - master

base_image: shippableimages/ubuntu1404_base

cache: true

before_install:
  - curl -sSL https://get.docker.com/ | sudo sh

install: true

script:
  - env | grep DOCKER
  - sudo make test
